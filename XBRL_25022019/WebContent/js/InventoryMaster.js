$(document).ready(function(){
	


/*$("#deprPercent,#marketValue,#amntPercent,#nominalAmnt,#originalCost").keypress(function (e) {
   
    if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {
      if($(this).attr('id')=="deprPercent"){
    	  $("#deperr").html("Enter Digit Only").show().fadeOut("slow");
      }if($(this).attr('id')=="marketValue"){
    	  $("#marketerr").html("Enter Digit Only").show().fadeOut("slow");
      }if($(this).attr('id')=="amntPercent"){
    	  $("#amnterr").html("Enter Digit Only").show().fadeOut("slow");
      }if($(this).attr('id')=="nominalAmnt"){
    	  $("#nomerr").html("Enter Digit Only").show().fadeOut("slow");
      }
    	return false;
    }
    });*/

});